import React, {Fragment, Component} from 'react'
import socket from '../socket'
import Axios from 'axios'
import {connect} from 'react-redux'

const MultiplayerHome = props => {
  return (
    <Fragment>
      <h1>List of Lobbies</h1>
    </Fragment>
  )
}
export default MultiplayerHome
